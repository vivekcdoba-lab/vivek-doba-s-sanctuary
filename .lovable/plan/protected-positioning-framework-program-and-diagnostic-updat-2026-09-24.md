# Protected positioning, framework, program, and diagnostic update

## Goal
Apply only the requested wording, framework, course identity, diagnostic-offer, and homepage metadata changes. Preserve all current layouts, colours, images, prices, GST logic, SEO coverage, forms, and working behavior except where the new copy or route explicitly requires a change.

## Changes

### 1. Centralize the new framework and diagnostic offer
- Add `src/data/framework.ts` exactly with Dharma, Artha, Kama, Moksha, their English meanings, and the supplied framework line.
- Add one reusable triangle display used on Home, About, and Score: gold outline; `Dharma · Health & values` at the top, `Artha · Business` bottom-left, `Kama · Relationships & team` bottom-right, and a softly glowing `Moksha · Legacy` centre point.
- Add shared diagnostic wording, WhatsApp message, and helper UI so every diagnostic button uses the same label, destination, and required explanatory line.
- Update the floating Support message to the supplied business-owner wording.

### 2. Apply positioning and trademark rules
- Use `Business Transformation Mentor for Entrepreneurs` wherever the public site introduces Vivek, including visible bio/identity lines and Person structured data.
- Add the supplied brand tagline and differentiator where Vivek/brand positioning is presented, without creating new sections or changing page layouts.
- Keep `Business Coach in Pune` in the homepage H1 and relevant search titles.
- Preserve `Success is a Triangle. Complete it.` and `Ghar bhi jeeto. Bazaar bhi.`
- Normalize public trademarks to ™ only, with ™ on the first visible mention per page and plain `Life’s Golden Triangle` / `Ram Nirvana` thereafter.
- Allow only `ॐ` and `सिद्धि साधना`; render the program as `Ram Nirvana™ (सिद्धि साधना)` on its first page/list mention.

### 3. Replace the public triangle model
- Replace public `Business · Health · Family` labels, chips, score language, and framework diagrams with the centralized Dharma/Artha/Kama/Moksha model.
- Keep ordinary course prose readable by changing `business, health and family` to `business, health and relationships` rather than inserting Sanskrit terms into running text.
- Update Home, About, Score, public course copy, public SEO descriptions, FAQs, and structured data that explain the method.
- Preserve unrelated assessment/account content unless it is a public explanation of this same framework.

### 4. Rename and migrate the program safely
- Rename the six-month one-to-one program to `LGT Platinum`, subtitle/stage copy `Master of Legacy`, and slug `lgt-platinum` in static course data.
- Keep its existing program content, price rules, and “Only 20 people a year”; apply only the requested business/health/relationships wording updates.
- Set the exact supplied SEO title and update its canonical, schema URL/name, menu, footer, contact options, course ladder links, next-step links, gallery/testimonial associations, sitemap/prerender route list, and admin-managed course record.
- Add a permanent client-side redirect from `/courses/lgt` to `/courses/lgt-platinum`; keep the old URL out of the sitemap while preserving it as an entry route.
- Update the database course row and related slug references so the public site and admin-managed content do not revert to the old name or slug.
- Change LGT Practitioner eligibility to `Graduates of LGT Platinum only` and its related locked-state copy.
- Rename Ram Nirvana as requested without changing its price, availability, or content.

### 5. Unify the diagnostic offer
- Change every diagnostic CTA to `Book a 45-min Diagnostic · ₹4,999`.
- Place the supplied one-to-one/GST/honest-fit line directly beneath each diagnostic button, including header, mobile header, homepage, About, Courses, and course CTAs.
- Use the exact supplied WhatsApp message for every diagnostic CTA.
- Remove only the separate homepage 45-minute-session band because it duplicates the unified offer.
- Preserve `Fee shared after your diagnostic call` wherever it currently appears.
- Update older SEO-page session CTAs that represent this same ₹4,999 offer so they use the unified diagnostic wording and WhatsApp destination.

### 6. Metadata and app-title cleanup
- Change the Apple web-app title to `Vivek Doba`.
- Set the homepage description, Open Graph description, and Twitter description to the exact supplied sentence.
- Remove the static `index.html` title because routed pages already own titles through the existing page metadata system; keep the page-specific title and social titles generated by that system.
- Preserve all other metadata, canonical behavior, schemas, images, and verification tags.

## Validation
- Run the TypeScript check and focused tests/static checks.
- Confirm the new and legacy course URLs resolve as intended, with `/courses/lgt` redirecting to `/courses/lgt-platinum`.
- Confirm Home, About, and Score use the shared four-part triangle and retain exactly one H1.
- Confirm every diagnostic CTA has the exact label, support line, and WhatsApp message, and the duplicate homepage session band is gone.
- Scan public source for forbidden ®, unapproved Devanagari, old framework labels, stale program slugs/names, and old diagnostic-button wording.
- Report every file where `Business · Health · Family`, `Life’s Golden Triangle` as the old program name, or `Book a diagnostic` still appears, distinguishing intentional method references and the preserved fee sentence from stale usages.
